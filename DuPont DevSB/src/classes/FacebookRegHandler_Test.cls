/**
 * Test Class for Facebook Registration Handler
 */
@IsTest 
public with sharing class FacebookRegHandler_Test {
    @IsTest(SeeAllData=true) 
    public static void testFacebookRegHandler() {
        // Instantiate a new controller with all parameters in the page
        FacebookRegHandler handler = new FacebookRegHandler();
    }
}